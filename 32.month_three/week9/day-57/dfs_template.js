function dfs(root) {
    if(!root) return;

    let left = dfs(root.left);
    let right = dfs(root.right);

    return somrthing(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;

    let left =dfs(root.left);
    let right = dfs(root.right);

    return something(left, right, root);
}


function dfs(root) {
    if(!root) return baseValue;

    let left = dfs(root.left);
    let right = dfs(root.right);

    return somrthing(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;

    let left = dfs(root.left);
    let right = dfs(root.right)

    return something(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;

    let left = dfs(root.left);
    let right = dfs(root.right);


    return something(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;

    let left = dfs(root.left);
    let right = dfs(root.right);

    return soething(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;

    let left = dfs(root.left);
    let right = dfs(root.right);

    return something(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;

    let left = dfs(root.left);
    let right = dfs(root.right);

    return soething(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;

    let left = dfs(root.left);
    let right = dfs(root.right);

    return something(left, right, root);
}

function dfs(root) {
    if(!root) return baseValue;
    let left = dfs(root.left);
    let right = dfs(root.right);

    return something(left, right, root);
}